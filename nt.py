import numpy as np
import matplotlib.pylab as plt 
from numpy import random

#panda builds on numpy library 


lst = [1, 2, 3, 4]
np_arr_1 = np.array(lst, )


#multidimensional array library = so list is one dimensional 


#lists are super slow due to lack of fixed types, arrays only store one type of value and use vectorization 
